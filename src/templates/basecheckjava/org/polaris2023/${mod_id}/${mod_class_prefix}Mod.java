package org.polaris2023.${mod_id};

import net.neoforged.bus.api.IEventBus;
import net.neoforged.fml.ModContainer;
import net.neoforged.fml.common.Mod;

import org.polaris2023.${mod_id}.utils.Helper;

@Mod(Helper.MOD_ID)
public class ${mod_class_prefix}Mod {
    public ${mod_class_prefix}Mod(IEventBus bus, ModContainer mod) {

    }
}